- for post in @posts
  = partial :post, :locals => {:@post => post, :@short => true}
%p.pagination
  - 0.upto(Index.pages) do |page|
    - if page == @page
      %strong= page
    - else
      %a{:title => "Go to page #{page}", :href => "/?page=#{page}"}= page
  
- @title = ''
